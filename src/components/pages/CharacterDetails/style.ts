import styled from "styled-components";

const CharacterImage = styled.img`
  width: 300px;
  height: 300px;
`;

const CharacterInfo = styled.div`
  font-size: 1em;
`;

export { CharacterImage, CharacterInfo };
